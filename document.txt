npm create tauri-app@latest 

npm install kaplay@3000.1.17

"dev": "tauri dev"
as we have two systems going on 
vite: bundler , bundles ur webcode and puts it on a local host that u can use 
tauri: takes this output & puts it in a desktop window

to enable full screen mode 

src-tauri/tauri.config.json mai add kr 
  "app": {
    "windows": [
      {
        "title": "scarybirds",
        "width": 800,
        "height": 600,
        "fullscreen": false, 
        "resizable": true
      }
    ],
  }

and then for enabling F11 as the hotkey 
us the get current window api key 
import { getCurrentWindow } from '@tauri-apps/api/window';
const appWindow = getCurrentWindow();